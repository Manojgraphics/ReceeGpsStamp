org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Release signing — upload keystore at app/keystore.jks. BACK THIS UP (losing it blocks updates).
# If this ever becomes a git repo, move these to ~/.gradle/gradle.properties so they aren't committed.
RELEASE_STORE_PASSWORD=ReceeGps@2026
RELEASE_KEY_ALIAS=rgs
RELEASE_KEY_PASSWORD=ReceeGps@2026
